# Contact Form Email Configuration
# Set the recipient email address (defaults to hello@fivetwentyfour.studio if not set)
CONTACT_EMAIL=hello@fivetwentyfour.studio

# Email Service Configuration
# Choose one of the following methods:

# Option 1: Resend (Recommended - Free tier: 3,000 emails/month)
# Get your API key from https://resend.com/api-keys
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxx
# For free tier, use: onboarding@resend.dev
# For custom domain (paid), use: your-email@yourdomain.com
RESEND_FROM=onboarding@resend.dev

# Option 2: SendGrid
# Get your API key from https://app.sendgrid.com/settings/api_keys
SENDGRID_API_KEY=SG.xxxxxxxxxxxxxxxxxxxxx

# Option 3: SMTP (Traditional email server)
# Configure your SMTP server details
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-email@example.com
SMTP_PASSWORD=your-password
SMTP_FROM=your-email@example.com

# Note: If none of the above are configured, form submissions will be logged to the server console
# for manual processing. At least one email method should be configured for production use.
